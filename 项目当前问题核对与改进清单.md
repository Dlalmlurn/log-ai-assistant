# 项目当前问题核对与改进清单

> **状态更新（截至 2026-06-16）**
>
> ADR-010（周期 baseline 与审核覆盖层）、ADR-011（运营控制面与可量化验收）已落地，本清单中的多项问题已被覆盖。下表给出当前覆盖度，详细问题描述仍保留在下文备查：
>
> | 清单项 | 状态 | 说明 |
> | --- | --- | --- |
> | #1 评分阈值/权重与文档不一致 | ✅ 已覆盖 | 新增 `config/risk-scoring-v1.json` 与 `scoring_version`，代码、文档、前端按 `76/51/26` 和 `25+35+20+10+10` 口径收口。 |
> | #2 反馈生效闭环 | ✅ 已覆盖（ADR-010） | 独立、版本化、可撤销的 baseline override 层 + 审核接口 `/api/v1/baselines/overrides`。 |
> | #3 多尺度基线 | ✅ 已覆盖（ADR-010） | `weekday / month_phase / calendar_month / rolling / global` 五周期 + 确定性 fallback。 |
> | #4 用户风险历史膨胀 / 窗口化风险 | ✅ 已覆盖 | 用户风险排行新增 `24h/7d/30d/custom` 窗口、误报剔除、有效风险分与 7 天半衰期衰减分。 |
> | #5 静态阈值与宽泛匹配 | 🟡 部分 | ADR-010 提供了"历史同期"数据基础；规则阈值标注验证、detector 状态持久化、关键词收敛仍未做（完整规则 DSL 已被 ADR-011 明确排除）。 |
> | #6 检得准（Precision/Recall/误报率） | ✅ 大部分覆盖 | 已有误报率、攻击检出率、高危检出率、可追踪率、检测/通知延迟，并新增 high-risk precision、攻击事件 recall、攻击类型混淆矩阵、风险等级准确率；消融对比仍可后续细化。 |
> | 成员C 阶段计数 / 攻击链验收 / 数据质量门禁 | ✅ 大部分（ADR-011） | 运营控制面统一调度 + 运行记录；数据质量门禁现已覆盖五项阻断阈值（含 `event_id_traceability_rate` 与 `consumer_lag`）。 |
> | 成员C URL 模板化 / 指纹归一化 | ✅ 第一轮覆盖 | 解析层已为 URL/path 资源在 `attrs` 写入 `url_template / resource_fingerprint / resource_normalization_version`，原始 `resource` 保留。 |
>
> **结论**：P0 的 **#1 评分统一标定** 与 **#4 窗口化风险** 已完成代码收口；#6 的准确率类验收指标已大部分补齐；URL 归一化第一轮已进入解析层；detector 启动 warmup 已可恢复短窗口状态并避免重复写历史异常。剩余短板主要是消融对比自动化，以及是否把 URL 模板独立列化。
>
> **实施规划**：未完成项的分阶段开发计划见 `project__notes/development/未完成改进实施规划.md`，当前实施分支为 `feature/project-issue-checklist-implementation`。

## 一、先纠正会议中的旧口径

1. **当前正式链路**

   正确链路是：

   `日志文件 → Filebeat → Kafka raw_logs → Flink → Kafka parsed_logs → ClickHouse`

   Flink位于两个 Kafka Topic 之间，负责标准化，不是 Filebeat 直接把数据送给 Flink。当前整体属于“以 Kappa 式实时链路为主，叠加 T+1 离线基线与质量统计”，不是完整 Lambda。

2. **event_id 的作用**

   `event_id` 用于事件唯一标识、去重、重放对账和全链路追踪，不能用来判断行为是否异常。异常检测依据应是时间窗口、规则命中、历史基线偏离和上下文证据。

3. **AI 已不是随机抽取**

   当前代码已限制 AI 默认只分析 `high`、`critical` 或状态为 `pending` 的异常事件，不再是无条件随机抽五条。人工选择“吃不准”的事件仍可作为后续交互补充。

## 二、目前最关键的问题

### 1. 评分代码与设计文档不一致

这是答辩前优先级最高的问题。

设计文档划分为：

- `low`：1～25
- `medium`：26～50
- `high`：51～75
- `critical`：76～100

实际代码划分为：

- `low`：0～39
- `medium`：40～69
- `high`：70～89
- `critical`：90～100

文档还把五项风险组成写成 `25+35+20+10+10`，代码中却允许 `rule_strength=70`、`behavior_sensitivity=30` 等值。这使“权重”到底是上限、占比还是经验分值不清楚。

**应改进：**

- 统一等级阈值，代码、文档、前端只保留一套。
- 明确每个维度的最大分数。
- 将规则分值和阈值迁移到版本化配置，而不是散落在代码中。
- 保存 `scoring_version`，保证历史结果可解释、可复现。
- 给每个典型场景制作“输入证据 → 分项得分 → 最终等级”的测试表。

### 2. 缺少真正生效的人工反馈闭环

当前已有 `/api/v1/feedback` 和 `ai_feedback` 表，但反馈写入后默认只是 `pending`，还没有完整实现：

```
人工确认 → 反馈审核 → 修正规则/基线 → 新版本评分
```

也就是说，目前完成了“反馈记录”，尚未完成“反馈生效”。

**建议增加：**

- 人工标签：`true_positive`、`false_positive`、`needs_review`。
- 记录审核人、理由、时间、关联规则和证据。
- 误报事件不删除，而是关闭或降权，保留审计记录。
- 连续多次确认正常后，生成“基线更新候选”，不能立即自动污染基线。
- 规则权重调整必须经过审核，并产生新版本。
- 页面增加“确认风险、确认误报、待观察”三个操作。

不建议直接采用“第一次×0.8、第二次×0.5、第三次归零”这种固定公式。应先积累反馈样本，再根据误报率、样本数量和置信度调整，避免攻击者利用反馈绕过检测。

### 3. 缺少日、周、月多尺度基线

当前已经不是单纯手工筛选样本：主路径会从 `security_logs` 生成日级特征，再使用最长90天历史生成用户基线，并记录均值、标准差、P50、P95、P99和置信度。

但它仍主要是“多天总体统计”，尚未形成导师要求的：

- 日内基线：小时分布、早晚差异。
- 周期基线：星期几、工作日与周末。
- 月度基线：月初、月末、结算周期。
- 历史同期：本周一上午与过去周一上午比较。

**建议数据维度：**

```
user_id + feature_name + time_granularity + weekday/month_period + time_bucket
```

检测时同时比较日、周、月三个尺度。只偏离日均但符合月底规律时应降分；同时偏离多个尺度并涉及敏感操作时才明显升分。

### 4. 用户风险可能出现历史膨胀

当前 `risk_score` 是单个异常事件的分数，不是直接永久累加到用户账户上。因此不能简单说“每触发一次，用户总分就永久加一次”。

但如果用户风险排行长期聚合全部历史异常，且误报不关闭、旧事件不衰减，最终仍会出现事实上的风险膨胀。

**应改成窗口化风险：**

- 最近24小时风险。
- 最近7天风险。
- 最近30天趋势。
- 已确认误报不进入有效风险。
- 历史事件使用时间衰减。
- 用户风险和单事件风险分开定义。

### 5. 规则仍有较多静态阈值和宽泛匹配

当前规则使用1、5、10分钟滑动窗口，这是合理起点；非工作时间和服务账号误报控制也已改善，会参考 baseline 和持久化来源。

仍需注意：

- 横向移动当前主要按同一用户10分钟访问至少3个主机判断，尚未充分比较用户历史同期主机数量。
- `download/export/admin/config/backup` 等关键词匹配较宽，正常操作也可能触发。
- 下载或导出行为可能单凭动作名称生成异常，缺少数量、数据量和同期基线支持。
- 内存滑动窗口在 detector 重启后会丢失短期状态，需明确可靠性边界。
- 规则阈值缺少真实标注数据验证。

### 6. 现有验证更偏“能检出”，缺少“检得准”

目前七类攻击场景可以进入日志、异常和部分 AI 阶段，证明系统能跑通、能检出；但还不足以证明评分合理。

**需要增加的指标：**

- Precision：报出的异常中有多少是真的。
- Recall：注入攻击中有多少被检测到。
- False Positive Rate：正常场景误报率。
- 各攻击类型混淆矩阵。
- 不同风险等级的准确率。
- 引入基线、反馈前后的消融对比。

测试集必须同时包含正常工作、夜班、运维维护、服务账号、月底财务导出等“容易误报的正常场景”。

## 三、成员C直接相关的问题

1. **阶段计数不完全独立**：Kafka、Flink、ClickHouse计数尚未全部来自各阶段真实指标。
2. **旧 ClickHouse 数据卷未迁移**：新环境采用 `ReplacingMergeTree`，旧环境可能仍为 `MergeTree`。
3. **生成器缺少周期语义**：工作日、周末、昼夜和月末行为差异不完整，会限制多尺度基线验证。
4. **攻击链验收是场景级覆盖**：尚不能证明每一条链都完整进入异常、AI和页面。
5. **URL动态参数未归一化**：加密串、ID、时间戳可能导致同类接口被视为不同资源。这里应优先做 URL 模板化或指纹归一化，例如把 `/user/123/detail` 转为 `/user/{id}/detail`。SCD和拉链表更适合记录用户、资产、规则等维度的历史版本，不是解决日志相似路径爆炸的主要方法。

## 四、陆组可共用的问题

陆组的 AI 模块也应统一纳入以下原则：

- AI 不进行第一轮异常发现，只研判系统筛出的高可疑事件。
- AI 输入必须包含规则、基线偏离、相关日志和窗口统计。
- AI 输出必须区分真实模型、mock和调用失败回退。
- AI 的意见不是事实标签，最终仍需人工确认。
- AI 建议只能进入 `pending`，审核后才能影响规则或基线。
- 需要记录模型版本、提示词版本、耗时、token成本和人工采纳率。

## 五、建议改进顺序

**P0：答辩前必须处理**

1. 统一评分阈值、权重上限和文档口径。
2. 准备三到五个完整评分算例。
3. 明确“已有反馈存储，尚未形成反馈生效闭环”。
4. 纠正 event_id、Flink位置和AI随机抽样等旧说法。
5. 区分事件风险与用户累计风险。

**P1：最有价值的功能改进**

1. ~~增加人工确认接口和页面。~~ ✅ ADR-010 override 审核接口已落地。
2. 让误报反馈影响风险排行与后续评分。（override 已可影响 effective baseline；尚未联动风险排行）
3. ~~加入日、周、月及历史同期基线。~~ ✅ ADR-010 多周期 baseline 已落地。
4. 增加时间衰减和评分版本。（**仍未做**，与 P0#1 一并推进）
5. 建立 Precision、Recall、误报率验证体系。（🟡 ADR-011 已有误报率/检出率/可追踪率/延迟；仍缺 Precision、混淆矩阵、消融）

**P2：后续工程完善**

1. URL模板化与资源指纹。（**仍未做**）
2. ~~三阶段独立计数。~~ ✅ ADR-011 运营控制面 + 数据质量门禁。
3. 旧表迁移。（迁移脚本 `sql/clickhouse/02_*`、`03_*` 已就位）
4. ~~detector 状态持久化。~~ ✅ 已增加启动 warmup，从 ClickHouse 最近窗口恢复内存滑动状态并记录稳定 anomaly id 去重。
5. ~~每条攻击链端到端自动验收。~~ 🟡 ADR-011 可追踪率验收已覆盖，逐链断言可继续细化。

## 六、当前最准确的总体判断

项目已经不只是“能采集和展示日志”：正式流式链路、T+1统计基线、持久化来源、规则检测、结构化异常、AI候选筛选和反馈存储都已经存在。

随着 ADR-010、ADR-011 落地，多时间尺度基线、人工反馈反向生效（override 层）、误报率/检出率验证体系均已具备。真正的核心剩余短板收敛为：

> **评分仍缺少统一标定与版本化（#1，P0），用户风险仍缺少窗口化与时间衰减（#4），验证体系仍缺 Precision/混淆矩阵类指标，URL 相似路径尚未归一化。**

后续改进主线应当是：

```
多尺度基线 → 可解释评分 → 人工确认 → 反馈审核 → 基线/规则版本更新 → 准确率复验
```
